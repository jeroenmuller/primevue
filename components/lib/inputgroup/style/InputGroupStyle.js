import BaseStyle from 'primevue/base/style';

const classes = {
    root: 'p-inputgroup',
    addon: 'p-inputgroup-addon'
};

export default BaseStyle.extend({
    name: 'inputgroup',
    classes
});
